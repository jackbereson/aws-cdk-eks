import chalk from "chalk";

export function InitRepeatJobs() {
  console.log(chalk.redBright("\n🚣 Generate Repeat Jobs"));

  // WithdrawByContract.create({})
  //   .repeatEvery("3 minutes")
  //   .unique({ name: WithdrawByContract.jobName })
  //   .save();

}
